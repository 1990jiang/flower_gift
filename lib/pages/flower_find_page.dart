import 'package:flutter/material.dart';

class FlowerFindPage extends StatelessWidget {
  

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("我是花现"),
      ),
    );
  }
}